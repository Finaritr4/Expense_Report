<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/primaryColorNight</item>
        <item name="textInputStyle">@color/primaryColorNight</item>
        <item name="textInputOutlinedStyle">@color/primaryColorNight</item>
        <item name="textFillColor">@color/white</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="backgroundColor">@color/darkBg</item>
    </style>
</resources>